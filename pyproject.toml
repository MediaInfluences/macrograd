[project]
name = "macrograd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "graphviz>=0.21",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
